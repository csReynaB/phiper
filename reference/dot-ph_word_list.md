# Internal helper: .ph_word_list

Build a human-readable list from a character vector, with optional
quoting and conjunction handling.

## Usage

``` r
.ph_word_list(word_list = NULL, and_or = "and", is_are = FALSE, quotes = FALSE)
```
