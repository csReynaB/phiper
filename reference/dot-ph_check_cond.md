# Internal helper: .ph_check_cond

Execute an expression with start/stop timing logs.

## Usage

``` r
.ph_check_cond(
  condition,
  error_message,
  error = TRUE,
  step = NULL,
  bullets = NULL,
  ...
)
```
