% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standard-backends.R
\name{.standard_read_memory_backend}
\alias{.standard_read_memory_backend}
\title{Build a \strong{memory-backed} \code{phip_data} object (no DuckDB / Arrow) with standard worflow}
\usage{
.standard_read_memory_backend(cfg, colmap)
}
\arguments{
\item{cfg}{Named list from \code{resolve_paths()}; must contain one
\code{data_long_path} element. Can be a directory or a file.}

\item{colmap}{List of colnames mapped to the reserved \code{phip_data}
colnames. Useful when the colnames in the file are named different than the
colnames of the \code{phip_data} object.}
}
\value{
A \code{phip_data} object whose \code{data_long} slot is a tibble in RAM.
}
\description{
Reads up one long data frame directly into R and returns an in-memory
\code{phip_data}. If the \code{data_long_path} is a directory, it reads all files in
the directory, assuming they have the same file structure. Heavy validation
of matrix content is deliberately
\strong{omitted} - that should be performed by the \code{phip_data} class validator.
}
\keyword{internal}
