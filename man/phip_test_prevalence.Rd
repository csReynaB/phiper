% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binary-analysis_peptides.R
\name{phip_test_prevalence}
\alias{phip_test_prevalence}
\title{Pairwise prevalence tests (Fisher + ratios) for each grouping variable}
\usage{
phip_test_prevalence(
  x,
  prev_list,
  group_cols = names(prev_list),
  prevalence_threshold = NULL,
  p_adjust = "BH",
  epsilon_prop = 0.5,
  epsilon_delta = 1,
  alternative = "two.sided"
)
}
\arguments{
\item{x}{phip_data object (used to compute per-group N = n_distinct(sample_id))}

\item{prev_list}{named list from phip_calc_prevalence(..., return = "wide")}

\item{group_cols}{character vector; defaults to names(prev_list)}

\item{prevalence_threshold}{numeric; optional pairwise filter (keep peptide if >= threshold in ≥1 of the two groups)}

\item{p_adjust}{method for p-value adjustment ("BH" default)}

\item{epsilon_prop}{numeric; pseudocount for proportion ratio (applied only to zeros), default 0.5}

\item{epsilon_delta}{numeric; pseudocount for delta-ratio (applied only to zeros), default 1}

\item{alternative}{fisher.test alternative ("two.sided", "greater", "less")}
}
\value{
nested list: \code{results[[group_col]][["G1_vs_G2"]]} = list(comparison_df=..., N=c(N1,N2), groups=c(group1,g2), group_col=gcol)
}
\description{
Pairwise prevalence tests (Fisher + ratios) for each grouping variable
}
