% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fold_change-analysis.R
\name{fold_qq_plot}
\alias{fold_qq_plot}
\title{Fold‑change Q–Q plot for \code{phip_data}}
\usage{
fold_qq_plot(
  pd,
  by = NULL,
  prop = 1,
  distribution = stats::qnorm,
  dparams = list(),
  xlim = NULL
)
}
\arguments{
\item{pd}{A \code{phip_data} object.}

\item{by}{Optional character vector of column names used for grouping.
The first element is mapped to \code{colour} / \code{fill}; users can
facet on any columns afterwards via the usual \verb{+ facet_*()}.}

\item{prop}{Proportion of rows (per group) to sample \strong{before} plotting
(numeric, \verb{0 < prop <= 1}).  Defaults to \code{1} (all rows).}

\item{distribution, dparams}{Passed straight to \code{stat_qq()} and
\code{stat_qq_line()} for alternative theoretical quantiles.}

\item{xlim}{Two‑element numeric vector passed to \verb{coord_cartesian(xlim = xlim)}.  Leave \code{NULL} to auto‑scale.}
}
\value{
A \code{ggplot} object ready for further layers / faceting.
}
\description{
A convenience wrapper around \strong{ggplot2}'s \code{stat_qq()} / \code{stat_qq_line()}
that produces a ready‑to‑modify Q–Q plot of the \emph{fold_change} column in a
\code{phip_data} object.
}
\examples{
library(ggplot2)
library(ggpubr)
pd <- phip_load_example_data()
p = fold_qq_plot(pd, by = c("group", "timepoint"), prop = 0.1) +
  facet_wrap(~group, nrow = 2) +
  ggpubr::theme_pubr()
}
