% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binary_alpha_plots.R
\name{plot_alpha_diversity_interactive}
\alias{plot_alpha_diversity_interactive}
\title{Plot alpha diversity (precomputed) — interactive (plotly)}
\usage{
plot_alpha_diversity_interactive(
  x,
  metric = c("richness", "shannon_diversity", "simpson_diversity"),
  group_col = "group",
  rank_col = "rank",
  filter_groups = NULL,
  filter_ranks = NULL,
  custom_colors = NULL,
  facet_by_rank = TRUE,
  ncol = 2,
  facet_scales = "fixed",
  interaction_only = FALSE,
  interaction_sep = NULL,
  x_order = NULL,
  x_labels = NULL,
  y_range = NULL,
  x_tickangle = 0,
  quartile_method = c("exclusive", "inclusive", "linear"),
  jitter_width = 0.25,
  point_size = 6,
  point_alpha = 0.85,
  text_size = 12,
  font_family = "Montserrat",
  show_grids = TRUE
)
}
\arguments{
\item{x}{a \code{"phip_alpha_diversity"} list (output of
\code{\link[=compute_alpha_diversity]{compute_alpha_diversity()}}) or a single alpha-diversity data frame.}

\item{metric}{one of \code{"richness"}, \code{"shannon_diversity"}, \code{"simpson_diversity"}.}

\item{group_col}{name of the grouping column in the alpha table
(default \code{"group"} when \code{group_cols = NULL} in the computation step).}

\item{rank_col}{name of the rank column (default \code{"rank"}).}

\item{filter_groups}{optional character vector; keep only these group levels.}

\item{filter_ranks}{optional character vector; keep only these ranks.}

\item{custom_colors}{named character vector of hex colors for groups (like ggplot scale_fill_manual()).}

\item{facet_by_rank}{logical; facet by \code{rank_col} if multiple ranks present.}

\item{ncol}{integer; number of columns in facet wrap (default \code{2}).}

\item{facet_scales}{\code{"fixed"}, \code{"free_x"}, \code{"free_y"}, or \code{"free"}.}

\item{interaction_only}{logical; if \code{TRUE}, plot only the interaction table
(when available in \code{x}). useful when \code{compute_alpha_diversity()} was run
with \code{group_interaction = TRUE}.}

\item{interaction_sep}{character; separator used to join interaction labels.
default is taken from \code{attr(x, "interaction_sep")} if present, otherwise \code{" * "}.}

\item{x_order}{optional character vector with desired group order (levels).}

\item{x_labels}{optional named character vector mapping group -> label (used on x axis).}

\item{y_range}{optional numeric length-2; y axis range (e.g., c(0, 2300)).}

\item{x_tickangle}{numeric; tick label rotation in degrees (default 0; e.g., 25).}

\item{quartile_method}{one of c("exclusive","inclusive","linear"); passed to plotly box (default "exclusive" ~ ggplot).}
}
\value{
A plotly htmlwidget.
}
\description{
native plotly version of \code{plot_alpha_diversity()}. mirrors the ggplot look:
per-group boxplots with jittered points and optional faceting by rank.
}
\examples{
\dontrun{
plot_alpha_diversity_interactive(df, metric = "richness", group_col = "group")
}
}
