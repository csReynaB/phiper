% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prevalence-POP-test.R
\name{summary.ph_prev_result}
\alias{summary.ph_prev_result}
\title{Summarize FDR accounting and design for \code{ph_prev_result}}
\usage{
\method{summary}{ph_prev_result}(object, ...)
}
\arguments{
\item{object}{A \code{ph_prev_result} object returned by \code{ph_prevalence_compare()}.}

\item{...}{Unused, kept for S3 compatibility.}
}
\value{
An object of class \code{summary.ph_prev_result} containing:
\itemize{
\item \code{overview}: tibble with one row per rank (\code{rank}, \code{POOL}, \code{PAIRS}, \code{m_r}).
\item \code{pairs_by_universe}: tibble with \code{group_col}, \code{k_levels}, \code{n_pairs}.
\item \code{pool_by_rank}: tibble with \code{rank}, \code{POOL}.
\item \code{totals}: named list with \code{PAIRS_total} and \code{m_total}.
\item \code{design}: named list with \code{paired}, \code{weight_mode}, \code{pop_k_min}, \code{fdr_scope}, \code{view}.
}
The object has a custom \code{print()} for nice console output.
}
\description{
Provides a compact overview of the testing design and FDR accounting:
\itemize{
\item per-rank pool sizes (\code{POOL_r}), per-universe level counts and pair counts,
\item the number of pairwise comparisons aggregated across universes (\code{PAIRS}),
\item the per-rank family sizes \code{m_r = POOL_r * PAIRS},
\item design flags (paired/unpaired), weight mode, \code{pop_k_min}, \code{fdr_scope}, and \code{view}.
}
}
\examples{
\dontrun{
s <- summary(res)   # where res is a ph_prev_result
s
}

}
