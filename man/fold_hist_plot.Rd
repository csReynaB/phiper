% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fold_change-analysis.R
\name{fold_hist_plot}
\alias{fold_hist_plot}
\title{Histogram of \emph{fold_change} for \code{phip_data}}
\usage{
fold_hist_plot(pd, by = NULL, prop = 1, bins = 50, xlim = NULL)
}
\arguments{
\item{pd}{A \code{phip_data} object.}

\item{by}{Optional character vector of column names used for grouping.
The first element is mapped to \code{colour} / \code{fill}; users can
facet on any columns afterwards via the usual \verb{+ facet_*()}.}

\item{prop}{Proportion of rows (per group) to sample \strong{before} plotting
(numeric, \verb{0 < prop <= 1}).  Defaults to \code{1} (all rows).}

\item{bins}{Number of histogram bins (default = 100).}

\item{xlim}{Optional numeric vector of length‑2 for x‑axis limits.}
}
\value{
A \code{ggplot} object.
}
\description{
Generates a histogram of the \code{fold_change} column, with optional per‑group
colouring/filling, sampling, and remote‑table friendliness.
}
\examples{
\dontrun{
fold_hist_plot(pd, by = "big_group", prop = 0.1, bins = 100) +
  facet_wrap(~big_group, nrow = 2)
}
}
