% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy-backends.R
\name{.read_memory_backend}
\alias{.read_memory_backend}
\title{Build a \strong{memory-backed} \code{phip_data} object (no DuckDB / Arrow)}
\usage{
.read_memory_backend(cfg, meta)
}
\arguments{
\item{cfg}{Named list from \code{legacy_resolve_paths()}; must contain the
four \verb{*_file} elements.}

\item{meta}{List returned by \code{legacy_prepare_metadata()} containing
\code{samples} (data-frame) and \code{comparisons} (data-frame or
\code{NULL}).}
}
\value{
A \code{phip_data} object whose \code{data_long} slot is a tibble in RAM.
}
\description{
Reads up to four wide matrices (\code{exist}, \code{fold_change}, \code{input}, \code{hit})
directly into R, pivots each to long format with \strong{data.table::melt()}
(faster than \code{stats::reshape()}), left-joins the available matrices on
\verb{(sample_id, peptide_id)}, attaches the cleaned sample metadata produced by
\code{legacy_prepare_metadata()}, and returns an in-memory \code{phip_data}.
Heavy validation of matrix content is deliberately \strong{omitted}—that should be
performed by the \code{phip_data} class validator.
}
\keyword{internal}
